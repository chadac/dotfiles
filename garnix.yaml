builds:
  - include:
    - "homeConfigurations.*"
    - "nixosConfigurations.*"
    branch: "nix-config"
  - include:
    - "homeConfigurations.*"
    - "nixosConfigurations.*"
    type: on-pull-request
